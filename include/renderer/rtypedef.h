/** ! 
 * Type definitions for renderer
 * 
 * @file renderer/rtypedef.h
 * 
 * @author Jacob Smith
 */

// Include guard
#pragma once

// Forward declarations
struct camera_s;
struct primitive_s;
struct shape_s;
struct spectrum_s;
struct integrator_s;

// Type definitions
typedef struct camera_s camera;
typedef struct primitive_s primitive;
typedef struct shape_s shape;
typedef struct spectrum_s spectrum;
typedef struct integrator_s integrator;
